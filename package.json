{
  "name": "subweb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "vite --host 0.0.0.0",
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "lint": "eslint .",
    "test": "node --test",
    "pages:check": "npx --yes wrangler@4.127.1 pages functions build functions --outdir dist/functions-test --project-directory . --build-output-directory dist",
    "format:check": "prettier --check . --ignore-path .gitignore",
    "check": "npm run format:check && npm run lint && npm test && npm run build && npm run pages:check"
  },
  "dependencies": {
    "vue": "^3.5.42",
    "vue-router": "^5.3.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@vitejs/plugin-vue": "^6.0.7",
    "eslint": "^10.10.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-vue": "^10.9.2",
    "globals": "^17.12.0",
    "prettier": "^3.8.4",
    "vite": "^8.2.2"
  }
}
